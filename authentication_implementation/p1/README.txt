PART 1

To run from command prompt: 

Go to the location where the jar files are saved. On 3 different windows, run the following commands.

1. Run the command java -jar KDC.jar
2. Run the command java -jar Bob.jar
3. Run the command java -jar Alice.jar

The communication messages for the extended Needham-Schroeder protocol appear on screen